# ── Default provider: Gemini (free tier, no credit card) ─────────────────────
# Get a free key at https://aistudio.google.com/apikey
GOOGLE_AI_API_KEY=

# ── Optional server-side fallbacks ────────────────────────────────────────────
# Users can also bring their own key via the UI (stored in their browser only)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
